@model List<BuildMonitor.Models.Feature>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@Html.ActionLink("Add new feature", "Create")

@if (Model != null)
{
    <table>
        <tr>
            <th>&nbsp;</th>
            <th>&nbsp;</th>
            <th>&nbsp;</th>
            <th>Id</th>
            <th>Feature Name</th>
            <th>Enabled</th>
            @foreach (var feature in Model)
            {
                <tr>
                    <td><a href="@Url.Action("Delete", new { id = feature.FeatureId})">Delete</a></td>
                    <td><a href="@Url.Action("Details", new { id = feature.FeatureId})">Details</a></td>
                    <td><a href="@Url.Action("Edit", new { id = feature.FeatureId})">Edit</a></td>
                    <td>@feature.FeatureId</td>
                    <td>@feature.FeatureName</td>
                    <td>@feature.Enabled</td>
                </tr>
            }
        </tr>
    </table>
}